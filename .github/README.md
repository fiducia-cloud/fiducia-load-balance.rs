# .github

GitHub-platform configuration for `fiducia-load-balance`.

- `dependabot.yml` — weekly dependency-update PRs for Cargo crates and GitHub Actions.
- `workflows/` — CI, container image publishing, and the secret-gated test-environment deploy.
